[prompt]
Define a function which takes an integer as it's parameter and returns its square.
[valid lines]
public int square(int x) {
    int y = x * x;
    return y;
}
[distractors]
    y = x * x;
    return x;
[end]